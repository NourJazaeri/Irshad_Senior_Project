# MongoDB Atlas Connection String
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority

# Server Configuration
PORT=5000

# JWT Secret for authentication
JWT_SECRET=your-super-secret-jwt-key-here

# Email Configuration (Gmail)
# EMAIL_USER: Your Gmail address (e.g., yourname@gmail.com)
# EMAIL_PASS: Your Gmail App Password (16 characters, NOT your regular password)
# To generate a Gmail App Password:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification (required for App Passwords)
# 3. Go to App Passwords (under 2-Step Verification section)
# 4. Generate a new App Password for "Mail"
# 5. Copy the 16-character password and use it as EMAIL_PASS
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-character-app-password

# JWT Token Expiration (optional, default: 24h)
# Examples: "1h" (1 hour), "24h" (24 hours), "7d" (7 days), "30d" (30 days)
JWT_EXPIRES_IN=24h

# Supabase Configuration (for PDF storage - optional)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key

# Python AI Service URL (FastAPI)
PY_AI_SERVICE_URL=http://localhost:8001


# Example MongoDB URI format:
# MONGODB_URI=mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/company_registrations?retryWrites=true&w=majority

# Supabase Setup Instructions:
# 1. Go to Supabase: https://supabase.com/dashboard
# 2. Create a new project or select existing project
# 3. Go to Settings > API
# 4. Copy the Project URL to SUPABASE_URL
# 5. Copy the anon/public key to SUPABASE_KEY
# 6. Go to Storage and create a bucket named "Irshad"
# 7. Make the bucket public by going to Storage > Irshad > Settings > Public bucket = ON
