# MongoDB Atlas Connection String
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority

# Server Configuration
PORT=5000

# Gmail-only Email Configuration (for sending login credentials)
# IMPORTANT: Gmail requires a 16-character App Password (not your normal password).
# Steps:
# 1) Enable 2-Step Verification on your Google Account
# 2) Create an App Password for "Mail"
# 3) Set the generated 16-character password as EMAIL_PASS below
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-char-gmail-app-password

# Example MongoDB URI format:
# MONGODB_URI=mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/company_registrations?retryWrites=true&w=majority

# Notes:
# - Gmail only is supported by the current configuration.
# - EMAIL_USER must be your full Gmail address.
# - EMAIL_PASS must be the Gmail App Password.
